energy.jygasoft.com {
	encode zstd gzip
	reverse_proxy web:3000

	# Cache largo para assets estáticos de Next.
	@static path /_next/static/* /static/*
	header @static Cache-Control "public, max-age=31536000, immutable"
}
